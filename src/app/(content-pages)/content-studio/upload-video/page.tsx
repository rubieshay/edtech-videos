import ContentForm from "../content-form";

export default function UploadVideo() {
    // UploadVideo allows the user to upload a new video
    return (
        <ContentForm existingVideoData={null}/>
    );
}